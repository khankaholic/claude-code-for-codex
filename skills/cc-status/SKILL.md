---
name: cc-status
description: Show active or waiting Claude Code jobs for the current repository, including estimated usage.
---

# Claude Code job status

Resolve the plugin root as two directories above this `SKILL.md`. Run `node <plugin-root>/scripts/cc-companion.mjs status` with an optional job ID from the request. Add `--all` only when the user requests completed history. Return the output directly.
