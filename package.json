{
  "name": "claude-code-for-codex",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Use Claude Code from Codex through a local companion runtime.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup": "node scripts/install.mjs --check",
    "setup:install": "node scripts/install.mjs --install",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.270"
  }
}
