---
name: cc-usage
description: Summarize captured Claude Code calls and estimated API-equivalent cost for the current repository.
---

# Claude Code usage

Resolve the plugin root as two directories above this `SKILL.md` and run:

```bash
node <plugin-root>/scripts/cc-companion.mjs usage
```

Describe dollar values only as client-side API-equivalent estimates, not subscription charges or authoritative billing. The plugin intentionally applies no budget ceiling.
